<?xml version="1.0" encoding="utf-8"?>
<!--
  Conversations, the PIN hash and the API key must not leave the device through
  cloud backup or device-to-device transfer: a lock is meaningless if `adb
  backup` or a phone migration hands the whole database over in clear.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>

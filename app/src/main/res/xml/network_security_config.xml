<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext stays allowed by default because a self-hosted Ollama on the LAN is
  reached over plain HTTP at an arbitrary IP, and Android's config cannot
  express "private ranges only".

  Every third-party host the app talks to is pinned to HTTPS instead, so a
  malformed URL or a hijacked DNS answer can never downgrade those calls.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">search.brave.com</domain>
        <domain includeSubdomains="true">wikipedia.org</domain>
        <domain includeSubdomains="true">open-meteo.com</domain>
        <domain includeSubdomains="true">openstreetmap.org</domain>
        <domain includeSubdomains="true">jina.ai</domain>
        <domain includeSubdomains="true">github.com</domain>
        <domain includeSubdomains="true">githubusercontent.com</domain>
    </domain-config>
</network-security-config>
